{
	"name": "@wtd/server",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run index.ts",
		"dev": "bun --watch run index.ts",
		"build": "echo 'No build step required for Bun projects.'"
	},
	"dependencies": {
		"@wtd/shared": "workspace:*",
		"pretty-bytes": "^7.1.0"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}